name: Giscus Comment Email Notify

# 监听 Giscus 评论:
#   - discussion(created):某篇文章/碎碎念的第一条评论会"新建 discussion",触发此事件(首评)
#   - discussion_comment(created):对已有 discussion 的后续回复
# 两者字段不同(comment.* vs discussion.*),下面用 github.event_name 三元表达式分流取值。
# 仅在发布仓 pppppst.github.io 运行(Discussions 与 SMTP secrets 都在此仓)。
#
# 收到评论后还会触发源仓(pst-blog)重建,刷新 commentCount,让评论区自动展开。

on:
  workflow_dispatch:
  discussion:
    types:
      - created
  discussion_comment:
    types:
      - created

jobs:
  send-email:
    runs-on: ubuntu-latest
    if: github.repository == 'pppppst/pppppst.github.io' && vars.ENABLE_COMMENT_EMAIL == 'true'
    permissions:
      contents: read
    steps:
      - name: Validate required secrets
        env:
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
        run: |
          test -n "$SMTP_SERVER" || (echo "Missing SMTP_SERVER" && exit 1)
          test -n "$SMTP_PORT" || (echo "Missing SMTP_PORT" && exit 1)
          test -n "$SMTP_USERNAME" || (echo "Missing SMTP_USERNAME" && exit 1)
          test -n "$SMTP_PASSWORD" || (echo "Missing SMTP_PASSWORD" && exit 1)
          test -n "$MAIL_FROM" || (echo "Missing MAIL_FROM" && exit 1)
          test -n "$MAIL_TO" || (echo "Missing MAIL_TO" && exit 1)

      - name: Send comment notification email
        uses: dawidd6/action-send-mail@611879133a9569642c41be66f4a323286e9b8a3b # v4
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          secure: true
          from: ${{ secrets.MAIL_FROM }}
          to: ${{ secrets.MAIL_TO }}
          subject: "[Blog评论提醒] ${{ github.event_name == 'workflow_dispatch' && '手动测试邮件' || (github.event_name == 'discussion_comment' && format('{0} 评论了你的文章', github.event.comment.user.login) || format('{0} 留下了首条评论(新建讨论)', github.event.discussion.user.login)) }}"
          html_body: |
            <h2>博客有新评论</h2>
            <p><strong>评论人：</strong>${{ github.event_name == 'workflow_dispatch' && 'manual-test' || (github.event_name == 'discussion_comment' && github.event.comment.user.login || github.event.discussion.user.login) }}</p>
            <p><strong>讨论标题：</strong>${{ github.event_name == 'workflow_dispatch' && 'GitHub Actions 手动测试' || github.event.discussion.title }}</p>
            <p><strong>评论内容：</strong></p>
            <blockquote>${{ github.event_name == 'workflow_dispatch' && '这是一封手动触发的测试邮件，用于验证 SMTP 配置是否正常。' || (github.event_name == 'discussion_comment' && github.event.comment.body || github.event.discussion.body) }}</blockquote>
            <p><a href="${{ github.event_name == 'workflow_dispatch' && format('https://github.com/{0}/actions/runs/{1}', github.repository, github.run_id) || (github.event_name == 'discussion_comment' && github.event.comment.html_url || github.event.discussion.html_url) }}">点击查看来源</a></p>
            <hr/>
            <p>Repository: ${{ github.repository }}</p>

  trigger-rebuild:
    # 收到新评论/新讨论时,触发源仓重新构建,刷新 memos 等页的 commentCount,
    # 让对应碎碎念/文章的评论区自动展开。需要发布仓 secret SOURCE_DEPLOY_PAT
    # (对源仓 pppppst/pst-blog 有 Actions:write 的 classic PAT,
    #  或 fine-grained PAT 授予 Actions 读写权限)。
    # 未配置则仅告警跳过,不影响邮件发送,评论区将在下次推送时刷新。
    runs-on: ubuntu-latest
    if: github.repository == 'pppppst/pppppst.github.io' && vars.ENABLE_COMMENT_REBUILD == 'true'
    permissions:
      contents: read
    steps:
      - name: Dispatch rebuild-on-comment to source repo
        env:
          GH_TOKEN: ${{ secrets.SOURCE_DEPLOY_PAT }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "::warning::SOURCE_DEPLOY_PAT 未配置,跳过触发源仓重建(评论自动展开将延迟到下次推送)"
            exit 0
          fi
          echo "触发源仓 rebuild-on-comment ..."
          gh api repos/pppppst/pst-blog/dispatches \
            -f event_type=rebuild-on-comment \
            -F "client_payload[source]=comment-notify" \
            -F "client_payload[event]=${{ github.event_name }}" \
            || echo "::warning::触发源仓重建失败,请检查 SOURCE_DEPLOY_PAT 权限"
